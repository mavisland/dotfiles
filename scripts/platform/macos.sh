#!/usr/bin/env bash
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

log "macOS platform bootstrap placeholder is ready."
log "Next step will add Homebrew, symlink, and app defaults support."
