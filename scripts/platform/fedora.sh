#!/usr/bin/env bash
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

log "Fedora platform bootstrap placeholder is ready."
log "Next step will add dnf-based package install and config wiring."
