#!/usr/bin/env bash
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/lib/common.sh"

log "Ubuntu platform bootstrap placeholder is ready."
log "Next step will add apt-based package install and config wiring."
